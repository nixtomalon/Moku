import 'package:flutter/material.dart';

class AnimeListTab extends StatelessWidget {
  const AnimeListTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Anime List Content'));
  }
}