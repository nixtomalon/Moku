import 'package:flutter/material.dart';

class SubmissionsTab extends StatelessWidget {
  const SubmissionsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Submissions Content'));
  }
}