import 'package:flutter/material.dart';

class MangaListTab extends StatelessWidget {
  const MangaListTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Manga List Content'));
  }
}